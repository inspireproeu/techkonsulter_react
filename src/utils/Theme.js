import { createMuiTheme } from '@material-ui/core/styles';

export const AppTheme = createMuiTheme({
  palette: {
    primary: {
      main: '#004750',
    },
    typography: {
      fontFamily: ['Poppins'],
    },
  },
});
